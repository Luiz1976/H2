# Variáveis necessárias para o backend standalone (sem valores reais)

# Porta do servidor
PORT=3001

# Origem permitida no CORS (ajuste para o domínio de produção)
CORS_ORIGIN=https://www.humaniqai.com.br

# URL do frontend (usada em redirecionamentos, se aplicável)
FRONTEND_URL=https://www.humaniqai.com.br

# Segredos devem ser configurados no ambiente (Render Dashboard)
# Exemplo de chaves utilizadas pelo projeto completo (opcional):
# JWT_SECRET=
# SENDGRID_API_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_PUBLIC_KEY=
# STRIPE_WEBHOOK_SECRET=