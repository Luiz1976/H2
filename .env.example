# Frontend (Vite)
# URL base da API (sem /api no final). Ex.: https://api.seudominio.com
VITE_API_URL=
# Fallback opcional para API (usado se a primária falhar). Ex.: https://h2-8xej.onrender.com
VITE_API_FALLBACK_URL=

# Backend
# Chave JWT para assinar tokens (NÃO colocar valor real aqui)
JWT_SECRET=
# URL de conexão do banco (Neon/Postgres). NUNCA commit valores reais.
DATABASE_URL=
# Porta do servidor Express (opcional)
PORT=
# Origem permitida no CORS (ex.: https://seu-frontend.com)
CORS_ORIGIN=

# Integrações (somente placeholders)
STRIPE_SECRET_KEY=
VITE_STRIPE_PUBLIC_KEY=
GOOGLE_API_KEY=
SENDGRID_API_KEY=

# Observações
# - Preencha estes valores somente em .env local/produção, nunca comite.
# - Não inclua /api no final de VITE_API_URL; o cliente já adiciona.
# - Em produção, use HTTPS e domínios válidos.