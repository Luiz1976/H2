# Ambiente geral
# NODE_ENV deve ser um de: development, test, production
NODE_ENV=

#############################################
# Backend (Node/Express)
#############################################

# Porta de escuta do servidor
PORT=

# URL de conexão do banco PostgreSQL (Drizzle)
# Formato típico: postgres://usuario:senha@host:porta/database
DATABASE_URL=

# Segredo JWT para assinar/verificar tokens
# Obrigatório em qualquer ambiente que use autenticação
JWT_SECRET=

# Origem permitida para CORS (frontend)
# Ex.: https://seu-frontend.vercel.app ou http://localhost:5000
CORS_ORIGIN=

# URL pública do frontend, usada em integrações (ex.: Stripe)
# Ex.: https://seu-frontend.vercel.app
FRONTEND_URL=

# Stripe (Backend)
# Chave secreta da API Stripe (NUNCA expor no frontend)
STRIPE_SECRET_KEY=

# Segredo do webhook Stripe (para validar eventos recebidos)
STRIPE_WEBHOOK_SECRET=

# IDs de preço dos planos no Stripe (price_xxx)
STRIPE_PRICE_ESSENCIAL=
STRIPE_PRICE_PROFISSIONAL=
STRIPE_PRICE_ENTERPRISE=

# Google Generative AI (Vertex/SDK), se usado
GOOGLE_API_KEY=

# Replit Connectors (SendGrid via Conector)
# Use UM dos tokens abaixo conforme ambiente:
# - REPL_IDENTITY (execução via Repl) OU WEB_REPL_RENEWAL (execução via Deploy)
REPLIT_CONNECTORS_HOSTNAME=
REPL_IDENTITY=
WEB_REPL_RENEWAL=

#############################################
# Frontend (Vite/React)
#############################################

# Base URL da API consumida pelo frontend
# Ex.: https://seu-backend.onrender.com ou http://localhost:3001
VITE_API_URL=

# Chave pública do Stripe para o frontend (publishable key)
VITE_STRIPE_PUBLIC_KEY=

# Observações:
# - NUNCA coloque valores reais neste arquivo.
# - Crie um arquivo .env separado por ambiente (dev/test/prod) e mantenha fora do controle de versão.
# - Garanta que backend e frontend apontem para os domínios corretos (CORS + VITE_API_URL).
